# Asterisk/SIP Configuration
ASTERISK_HOST=localhost
ASTERISK_PORT=5060
ASTERISK_USERNAME=ai_service
# IMPORTANT: Use a strong password (min 12 chars, no weak patterns)
# setup.sh will auto-generate a secure password for you
# To generate manually: python3 -c 'import secrets; print(secrets.token_hex(24))'
ASTERISK_PASSWORD=

# Ollama Configuration
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=llama2

# Service Configuration
SERVICE_HOST=0.0.0.0
SERVICE_PORT=8000
RECORDINGS_DIR=./recordings
MIN_FREE_SPACE_MB=100
